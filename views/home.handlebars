<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Main page</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <div class="Index">
      {{#robots}}
        <section class ="bigBox">
          <img src="{{avatar}}" alt="">
          <a href="/users/{{username}}"><h3>{{name}}</h3></a>
          <section>
            <p class = description>{{job}} at {{company}}</p>
          </section>
        </section>
      {{/robots}}

    </div>
    </script>
  </body>
</html>
